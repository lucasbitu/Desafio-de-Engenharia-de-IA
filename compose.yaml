services:
  classifier:
    build: .
    ports:
      - "8501:8501"
    environment:
      ENABLE_LLM_JUSTIFICATION: ${ENABLE_LLM_JUSTIFICATION:-false}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5-mini}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    restart: unless-stopped
